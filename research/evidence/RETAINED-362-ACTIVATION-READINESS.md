# Retained362: activation audit and conditional qualification boundary

2026-09-09, after completed363/364. Read-only source/integration audit, not a new
SCORE trial. Preservation answers: no designed function removed, disabled,
deferred or reduced; nothing deleted. Production remains c76a8ea/accepted258.

## The user's proposed daily guard already exists

Canonical btc_protocol.cpp:209 computes the authoritative deadline as
min(endsAt,receivedAt+daySeconds[day]); absent endsAt returns the configured
checkpoint deadline. btc_main.cpp:2204--2217 compares this deadline against
receivedAt+effective_response_budget_ms(options), capped at5000ms. At the canonical
5000 setting a daySeconds<=5 day cannot authorize public continuation. A nominal
longer day received too late also cannot authorize it. Actual work additionally
needs time remaining after transport reserve; nominal>5 alone is not a promise
that the suffix runs or gains anything.

Frozen360 host:2394--2406 reads the research control only after the original
public refinement and only if publicContinuationAuthorized and nonterminal.
It runs resource marginal work only if enabled and the original public work has
not reached its deadline. The resource routine is therefore already an optional
longer-window suffix. Adding another>5000 switch would not repair the inactive
independent-run differences. It would duplicate an existing guard, not supply a
new general improvement. No guard edit was made.

362's three5s score losses are observed independent-run outcomes with no resource
entry.363 locates their first divergences in main/F0 or the original protected
refinement. This is NOT evidence that the resource optimizer executed in5s, nor
proof that those differences are harmless random noise. The original362 verdict
and all adverse results remain intact. Compiled conditional operation equality
under equal exogenous schedules is distinct from identical independent timed runs.

## Persistent state across a longer day then a short day

Normalized source comparisons found exact equality between canonical and frozen360
for the full load_replay_resume function, run_http initialization/new-day guards,
and serialized submission/retry/ACK/post-ACK tail. No binary equivalence between
Windows production and Linux research is inferred from these source comparisons.

The runtime keeps checkpointClosedLoopActive when an earlier accepted continuation
has caused checkpoint divergence, even if today's publicContinuationAuthorized is
false. It therefore replays today's complete checkpoint on authoritative richer
state with independent validity and dominance checks, without running a new public
suffix. Accepted actions advance actual/checkpoint/virtual ledgers once. The
deliberately expired-day path resets all three branches to the exact server-WAIT
transition.364 verifies that latter path after actual strict-gain history.

No new state-ownership or activation defect was established. Do not equate this
audit with live unexpired mixed-window coverage:360 mixed days had no strict gain;
364 strict-gain history used an expired next day. Nor does a richer current state
prove arbitrary adaptive future search cannot choose differently. Certificates
and protected checkpoint ownership remain required.

## Decision, not retrospective promotion

Keep exact candidate at artifacts/research/360/completed/btc360-plain:
D3684DC3F4ADB5E6E74F5E75412CCB76E681D21F760B6BC0FA61EDC25A029678.
Canonical build-release/udonshield_btc.exe remains
F97F168FE76FEF1B6226D2C2CFDAF954F116D39F427B6F157594151C461E1275.

362's10/15s22/26/0,+90 is promising consumed development evidence. It is not
qualification for arbitrary>5/>10s, all official maps or future days. Official
configuration expected from the user tomorrow is not yet available. Retention
does not change rejected362, consume its sealed54/108, or enable production.

If authoritative configuration materially defines a longer-window target domain,
evaluate a separately preregistered qualification with explicit scope, fresh
unopened evidence, all independent-run uncertainty/downside retained and unchanged
5000 main/checkpoint/short-day capability. Do not cherry-pick prior winners or
repeat the old matrix for luck. Before any promotion also require exact canonical
source integration, protected multi-team lanes, unexpired mixed-window lifecycle
where applicable, full tests and genuine authenticated BTC target-host telemetry.
Changing mechanism or hard-enabling/recompiling is not covered by the frozen-byte
contract. No guarantee of qualification before tomorrow is justified.

No timed job remains active after364. No new general algorithmic fix emerged from
363 or this audit, and no practical-ceiling/championship claim is made. Preserve
the candidate and evidence for the authoritative domain decision instead of
inventing another shortlist-order or cutoff experiment without a new invariant.

## Subsequent finite coverage365

365 now passes four frozen-byte sessions after strict-gain history with a live
5000ms authoritative remaining window, then normal terminal day:8newACK,18replay
days14transitions,zero expired WAIT/safety failures. This closes the specific
normal-submission omission described above; setup nominal windows stay10/15s,
with state endsAt shortening the next day. It does not establish arbitrary
mixed-window score equivalence or real BTC integration. See365-closure and exact
completionCBB77A9B789B87F1A0D5AA06F9B66BF3EEA1E62FAB9C03F7FFEAE58D4336771D.
No measured job remains after365. Assess authoritative official setup before the
match when supplied; no need to wait for a completed official loss to examine it.
