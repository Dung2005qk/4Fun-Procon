# 341 protected execution contract closure — 2026-09-07

No designed production function is removed, disabled, deferred or reduced.
Nothing is deleted. Production source and both frozen binaries stay unchanged.

The first contract-only preflight finished the parent HTTP process with four
valid actions and zero child stderr, then the NEW transport checker incorrectly
required candidate-only `horizonPricing` telemetry from canonical parent c76.
This is a harness schema error, not a bot failure. All original preflight files
are preserved under `341-protected-http-preflight`; they are invalid preflight
closure evidence, not protected score results. No protected case ran there.

The unscored harness now takes an explicit binary side. Parent must have the
canonical profile schema; candidate still must have complete pricing counters
and pass every frozen counter/failure check. The checker does not fabricate
zero counters for either binary. A regression test ensures that the same parent
replay fails if passed as candidate. The frozen development/holdout harnesses,
their results and source candidate are not modified.

Corrected contract-only preflight `341-protected-http-preflight-v2` completed
both sides, eight valid actions and six reconciled transitions. Its create-only
completion SHA256 is
`5A2C2DB08E0862779F4B55B7B4CEC57B2D41F3BB5B5A1DFBADCCD70DE723F298`.
Unit fixtures exercise native assignment, fixed assignment, per-day windows
5/10/15/5 seconds, real Windows WinHTTP, exact canonical simulation and independent
validation, plus own/external two-day road traffic. The new bridge reproduces
the old bridge's full exact transition while additionally returning its already
computed exact road footprint. No new planner or oracle is in the transport.

The registered 108 protected pairs cover 684 days per side: 216 side results,
108 pair markers, 1368 valid actions and 1152 transitions. Public day windows
alone total 13680 seconds (3h48m), plus role-selection, process and polling
overhead. Cases and paired order remain the inputs frozen before source change.
Fixed roles are explicit synthetic pre-match assignments, not forged accepted
days; native roles use real MatchSession selection and HTTP assignment.

Freeze detailed evaluation before measurement: official lexicographic W/T/L,
first-tier deltas, every gain/loss tail and family/fuel/map/role/horizon/player/
window/road/agent/stock/brand/step stratum. The conservative protected screen
requires zero operational failure, no lifetime/daily component loss, worst
serving loss at most one, no negative serving net in a family or fuel stratum,
and no unactivated score difference. If pricing never improves a certificate,
compare exact plans, roles, final agent states, ledgers and road footprints;
differences block an equivalence/causal claim, not permission to tune a cutoff.
Do not invent a fresh win quota in inactive lanes: independent benefit already
passed development and one-time holdout. Small losses are reported and evaluated,
not mechanically rejected merely for being losses. This screen NEVER authorizes
promotion by itself; relevant lane-champion comparison, production-integration
equivalence, full tests and genuine BTC target-host gates remain mandatory.

Atomic resume skips only hash-verified complete sides. Missing result with any
side prefix, foreign data or inconsistent pair markers is a blocker. A clean
memory-floor stop can resume with the same frozen runner after RAM recovers and
no runner/child is active; an interrupted accepted-day prefix cannot be blindly
replayed. Preserve every file and request a targeted recovery audit.

This server is synthetic loopback, not official BTC. Frozen aggregate external
traffic is not a population of active opponent policies. Timing is NOT performance
or tournament-readiness evidence. Linux HTTP remains unsupported, so moving this
screen to the existing Linux VM or switching to sandbox changes the runtime under
test. The VM already passed independent C++ unit and 17 compiler-parity cases.
