# 366: sequential qualification of retained362, before any two-agent work

Registered 2026-09-09 before generating or observing366 evidence. User explicitly
requires resolving362 and qualifying a new parent first; two-agent research is
not authorized concurrently. No366 pair probe, candidate or implementation exists.
The obsolete empty monitor was deleted. Register a monitor only after a real job.

Preservation answers: no designed product functionality removed, disabled,
deferred or reduced; nothing in the product is deleted. This is an explicitly
changed prospective evaluation protocol, NOT a new optimizer or a claim that362
passed its old protocol. All362 adverse evidence and its rejected verdict remain.
Canonical c76a8eaa4f200e3eeeb1a58ef1d1fb3d0c13579c/accepted258 remains unchanged.
No build, reserve reclaim, RAM threshold, main/role budget or production change.

## Reopening decision and limits of exoneration

The user requests reassessment of whether an inactive, treatment-blind path was
being rejected mechanically for independent timed-run variation.360 provides the
normal compiled control-flow boundary: the control is read only after original
public work, under authoritative-window and nonterminal guards.363 locates all
three362 short-window score losses before that read.364/365 now additionally
validate restart, expired and normal short-window execution after real certified
gain history. This completes a concrete cross-history lifecycle coverage gap;
it does not prove whole-program determinism or identify the exact OS clock event.

Conclusion: within the documented equal-exogenous-input model, changing only the
late flag cannot change a wholly inactive match. Independent runs need not tie.
The old gate that requires every observed inactive stratum's sum to be nonnegative
does not test that causal proposition.366 explicitly replaces THAT prospective
inactive statistical gate, while retaining all actual observations, overall
benefit, active-lane downside, causal certificate and production gates. This is
not permission to discard short-window losses or to widen an optimizer's scope.
No official configuration is assumed and no waiting for tomorrow is required.

## Frozen mechanism and fresh evidence

Use exact360 plain binary, control0/1, no recompilation:
D3684DC3F4ADB5E6E74F5E75412CCB76E681D21F760B6BC0FA61EDC25A029678.
Its resource function is the unchanged362 single-agent suffix: query-only fuel
expenditure conditioning, target/marginal API,32 routes,1.25M states,4workers,
80ms validation reserve and at most one exact strict takeover after ALL original
public nonterminal work. Every original one-agent/pair/tanker/terminal/W1/ACK/
retry responsibility remains present. No two-agent extension in366.

Fresh grids use seed pool202609093660000, excluding all350/347/344/357/362
development and sealed gameplay identities without inspecting sealed outcomes.
Reuse the existing validated fixture generator and counterbalanced four-side
orders, not any selected winners. DEV36, holdout54, protected108 fixtures; each
has parentA/candidateA/parentB/candidateB in its frozen balanced order. Same six
families, three fuels,8/32 maps,5/10/15s windows,4/5/10 days,8/9/10teams and
fixed/native roles. Freeze every split hash, runner, classifier and dependencies
before measuring. Only DEV uploads now. Existing authorized VM only.

## Prospective gates (same in every stage unless stated)

1. All original exact simulator, independent validator, transition/ledger,
   checkpoint, role/main5000 and authoritative deadline/submission safety gates.
   Zero unexpected failure.360/361 frozen boundary plus364/365 lifecycle hashes
   must pass. No suffix frames or treatment consumption on wholly5000 matches.
2. For EACH repeat: every official aggregate component over ALL rows is
   nonnegative. Active-window benefit uses ONLY10/15s: componentwise nonnegative,
   at least one positive, wins>losses, gross serving gain>=2*gross serving loss.
   Every active public stratum is componentwise nonnegative. No weighted tiers.
3. No individual lifetime or daily-distinct loss in ANY lane. For active rows,
   retain362's serving-loss bound max(1,parentServings/50), without noise relief.
4. Inactive rows: retain every score/trajectory difference and both A/A controls.
   For each fixture the serving tail may not exceed the larger of the original
   2-percent-or-one allowance and the absolute serving spread observed between
   that SAME fixture's same-arm repeats. Never borrow another fixture's noise.
   This is a finite empirical envelope, not proof of a noise cause or future tail.
5. Inactive statistical unit is the FIXTURE, not two independent repetitions.
   Normalize each repeat's serving delta by max(1,parentServings/50), and average
   the two normalized deltas for that fixture. Require at least12 fixtures.
   One-sided exact sign test of median normalized delta>-1 must have p<=0.05;
   boundary ties contribute to neither side and lack of power fails. Separately,
   an exact sign test detecting a negative median at zero with p<=0.05 blocks as
   unexplained systematic shift, EVEN IF the noninferiority test passed. Report
   both tests, all raw first tiers/tails and every inactive/public stratum. These
   tests describe finite evidence, not mathematical equality or a mean guarantee.
6. Repeatable causal winners>=2DEV,>=4holdout,>=2protected; span>=2families in
   DEV/holdout, and both10/15s in holdout. A win counts only if a same-invocation
   independently validated strict suffix takeover precedes first divergence.
   Holdout/protected require robust active exact-sign p<=0.05: repeatable causal
   nonoverlapping wins versus ALL nonoverlapping active losses. Unresolved and
   noncausal gains are reported, never promoted into causal wins.

No partial score inspection, optional stopping, additional lucky repeats or
post-result threshold/stratum/metric changes. Failed or ambiguous stage stops;
retain evidence and do not automatically reseed. No366 score is computed from362.
Only completeDEVpass opens366holdout once; completeholdoutpass opens366protected
once.362's sealed54/108 remain untouched and cannot qualify366.

## Product sequence

After ALL fresh stages pass, integrate only this exact frozen mechanism into
the canonical production path, verify source/operation equivalence, full unit
and protected multi-team gates, then genuine authenticated BTC target-host
lifecycle/latency/safety telemetry before any commit or parent declaration.
If integration changes the mechanism, existing366 qualification does not cover
it. Retaining a Linux research binary is not Windows production equivalence.
Only after a new parent is actually accepted may two-agent research begin from
it. No promise of success or completion before tomorrow's match is justified.
