# 370 — exact resource suffix integration and BTC readiness

2026-09-10. User explicitly asks to complete the remaining checks so362/366 can
be ready for a parent decision. This authorizes isolated integration and BTC
validation now; it does not turn the original366-P2 gate into a pass or remove
its two observed active losses. No new score mechanism, matrix, holdout or
repeated consumed-case search is authorized by this registration.

Preservation: no designed function is removed, disabled, deferred or reduced.
Nothing is deleted. Existing258 main/checkpoint/refinement/tanker/terminal/ACK/
proof capabilities remain intact. Installed258 binary remains unchanged while
the isolated candidate is built and tested. No RAM-floor or reserve change.

## Exact scope

Parent c76a8eaa4f200e3eeeb1a58ef1d1fb3d0c13579c. Windows installed binary
F97F168FE76FEF1B6226D2C2CFDAF954F116D39F427B6F157594151C461E1275.
Copy the already-frozen357 resource method and header exactly. Keep all other
library sources identical to parent. Mechanically insert the frozen360 resource
call and diagnostics into the original native WinHTTP host; remove only the
research environment toggle, replacing it with the same public-authorized and
nonterminal guard at its original late location. No startup/main treatment read,
second solver, new dispatcher, changed query cap or altered certificate.
Prove full host equivalence to frozen360's policy after this explicit integration
transformation and removal of its isolated process-RPC transport shim.

Frozen method source76A34E707AFD2466919E7911D5B554581F621AD407FEDA720DB061979CA601F2;
header633E394F02710050AD88BAAD5F80EADCA46B2569998FFE6D0C36347538FDED3D;
host4031F7D3A73E6D4D0ADF4182B332CAC9C494726E05B9500F7C77B27EF4BDC28A.

## Checks and stop boundaries

1. Isolated source snapshot, exact source/host reversal and hash manifest before
   execution. Authoritative MSVC/Ninja/CMake from build-release/CMakeCache.txt.
2. Full existing unit suite, strategy smoke and independent master oracle smoke.
   Reuse the frozen357 seven-contract witness for exact strict gain, no-gain,
   expired/incomplete/terminal preservation, bounded work and input immutability.
   Compare frozen method code, not independent wall-clock scores on old matrices.
3. Fresh genuine BTC operational checks: hard, exactly3 bots. One4-day5000ms
   inactive checkpoint match and one6-day10000ms active public-window match,
   respectively8x8/3agents/8spots/4brands and24x24/6agents/24spots/8brands;
   daySteps48/60 and default fuel. These are lifecycle/counterexample gates,
   not promotion-strength evidence. Connect immediately after creating each.
   No other local computation while a match is active. Existing saved login/token
   only; never record credentials. No match/config tuning to force a takeover.
4. Preserve exact executable/replay hashes, config, official scores/rank, every
   ACK/transition, checkpoint/main/public timing, resource certificates/work and
   safety/deadline flags. No source changes mid-match. A loss is retained and
   classified, not erased by a repeat. Ambiguous accepted-day recovery blocks
   blind restart. Live unexpected invalidity/deadline failure blocks readiness.

No automatic parent/commit solely from green integration or bot rank1. At the
end make an explicit release recommendation separating measured score benefit,
retained366-P2 failure, residual indirect timing uncertainty, and integration/BTC
readiness. This record is not a retrospective score-gate waiver. Two-patrol
research remains sequential after parent decision. No new monitor for short BTC
matches, no cloud resources needed for native target-host integration.
