# 360: compiled late-treatment boundary contract (not SCORE)

Registered 2026-09-09 following complete 359. This is an isolated contract build
and tiny synthetic lifecycle test, not a new candidate qualification matrix.
Canonical c76a8ea/accepted258 and frozen357 remain untouched; no product commit.

Preservation: no designed function is removed, reduced, disabled or deferred.
Both experimental consumers retain their exact Boolean activation semantics.
Only the redundant startup storage/read is replaced by the equivalent late
control, wired to optimizer AND telemetry in the same isolated edit. Preserve
all planner code, existing control flags, timing constants, result telemetry,
source/branch consumers, ACK/retry and post-ACK work. No reserved time reclaimed.

## Pinned inputs and allowed edit

357 execution F0188694FF3E0042D1B85F8111F06E42AF7F1538A71C6AECBB44F384ED4C102C,
original Linux binary 083B134AD9E0AFE847DDF8E95B7E4352F5F3E3CA5E6F281D3D83DB2B7961144D,
frozen library 3449726097224731A4F318020D78D11EA185C733568CD00C033155699AD29796.
359 report F1B21E5DD29EDA4FF209C9CD275F38685CCCF3F54B9F9D9310910DB904566024.

Copy ONLY portable btc_main.cpp into artifacts/research/360. Restore canonical
RuntimeOptions layout and startup parsing. Initialize a local treatment flag false
after the complete checkpoint. Inside the existing richer-checkpoint region,
after the full original public refinement returns, read the same experimental
environment name ONLY if public continuation is authorized and day nonterminal.
Use that value for both existing consumers, preserving no-entry telemetry.
Algorithm implementation, caps, worker count, validation and rollback are unchanged.

Reuse the frozen357 static library and include tree; compile only the host object.
Build plain and getenv-interposed executables from that SAME host object. The
interposer logs only reads of the non-secret research flag, plus wall timestamp;
never token values. It delegates all getenv calls unchanged. Instrumentation is
contract-only and cannot claim competition latency or influence a score verdict.

Use only existing authorized VM udon-f0-240-0829, project
project-10f9c8e1-a791-46a2-920, us-east1-b; scoped /home/LMC/udon360-0909.
Verify frozen357 dependencies and no competing research solver before starting.
No VM creation/deletion or source-library rebuild. Freeze uploaded files, command,
host object, executables and the four contract setups before executing them.

## Finite lifecycle contract

Use test_protected_http_341.contract_case (8x8,3agents,2spots,8teams,4days),
already an explicitly synthetic operational fixture, not development/holdout
score data. Two configurations, each at experimental control 0 and 1:

1. fixed-all-Patrol, daySeconds=[5,5,5,5]; expect zero research-control reads.
2. native roles, daySeconds=[5,10,5,10]; expect exactly one read on day2, none
   during startup/roles or days1/3/4 (day4 is terminal).

Full original process-RPC/traffic/dual-validator/ACK runtime, no fake-clock solver,
fixed-role substitution in the native case, search cap reduction or worker
serialization. Total four matches and16 accepted days. Preserve every output;
never repeat an accepted partial side. A mismatch stops this contract with exact
evidence; no blind rerun. After completion rerun only read-only verification.

Static checks must show that every treatment read is within the intended guard,
both consumers use the same local value, all original daily prefix and ACK-tail
source is unchanged, and library/headers remain exact. Inspect compiled host and
linked call site; retain disassembly and wrapper reference. Pure Boolean checks
from359 are necessary but not a substitute for these executable checks.

Completion does NOT prove literal independent-clock trajectory equality, full
OS/allocator/scheduler noninterference, or a stronger product. It can support a
prospective qualification design only after remaining obligations in359 are
resolved, including mixed-window persistent state and removal of instrumentation.
No SCORE/holdout/protected stage automatically follows.357 stays inconclusive,
its sealed54/protected108 unopened. New SCORE inputs/gates must be preregistered
separately. Never suppress a loss, change the official comparator or waive a gate.
