# 345 read-only selected-witness realization attribution

Registered2026-09-08 after complete344development verdict, before new attribution.
Parent c76a8ea/accepted258 unchanged. Rejected344development gives three losses
replicated in both repeats despite earlier strict certificate improvement. 333's
older332data found zero value loss at ACK/next-selection boundaries; that closure
is retained and cannot be generalized blindly to fresh344counterexamples.

Preservation: no function removed, reduced, disabled or deferred; nothing deleted.
No product source/binary change, planner/oracle invocation, HTTP match, new fixture
or holdout inspection. Reuse the frozen exact314 bridge for deterministic replay
of recorded plans only; both simulator and independent validator must agree.

Include ALL24development fixtures, both repeats, both binaries:96matches,
288nonterminal boundaries and864current-plus-suffix dual-validation days. Verify
the complete344report and original artifact hashes. Full selected W1 suffixes
must be certified, replay to their declared score, and start from exact actual
ACK agents/ledger. At every boundary record cache presence at ACK/before next,
next selected certificate, current-floor eligibility, actual final score, pricing
work, audit disposition and exact next-plan identity. Also record same-invocation
base/final candidate selection and all improved outcome scores. Group by the
complete original W/T/L and all available family/player/repeat/side/day strata.

Questions, with no assumption of the answer:
1. Is a valid better suffix lost at ACK, idle/cache, next shortlist or selection?
2. Is every next certificate and final result >= the inherited witness, leaving
   a lower-bound ranking/option-value gap rather than broken consumption?
3. Does current-day floor exclude a future-better certificate? Do not bypass it.

Any dual-validation/provenance/state mismatch blocks conclusions. A demonstrated
same-state eligible retained certificate loss on at least2distinct fixtures and
2families permits a separate general consistency proposal, not implementation or
promotion. If absent, close that consumer hypothesis and identify the remaining
ranking/option-value mechanism from full source/evidence before proposing a new
experiment. No retuning344, no reopening333, no arbitrary matrix rerun, no claim
that a certificate lower bound proves actual-policy dominance. Freeze this note,
script/tests, directly relevant source and every input before executing345.
