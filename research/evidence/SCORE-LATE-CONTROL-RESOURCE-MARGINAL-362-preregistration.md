# 362: prospective qualification of frozen late-control resource marginal suffix

Registered2026-09-09 AFTER360/361 contracts and BEFORE any362 fixtures/results.
Canonical direct parent:c76a8eaa4f200e3eeeb1a58ef1d1fb3d0c13579c/accepted258.
Candidate:exact360 plain Linux binary
D3684DC3F4ADB5E6E74F5E75412CCB76E681D21F760B6BC0FA61EDC25A029678.
Same executable for control0/1; no interposer, no new source edit or build.
Library3449726097224731A4F318020D78D11EA185C733568CD00C033155699AD29796.
No designed functionality removed/disabled/deferred/reduced;nothing deleted.
All existing main/roles/global/target/pair/tanker/refinement/ACK/retry/proof and
terminal258 duties remain unchanged. No transport reserve reclaim or RAM floor.

## Reopen basis and explicit difference from357

357 supplied broad causal gains but was INCONCLUSIVE under its frozen literal
independent-clock exact5000 pairing gate. It remains so; its sealed54/protected108
remain unopened and cannot rescue362.359 identified actual treatment reads in
inactive357 runtime.360 moves the sole read behind full original public work and
compiled authorization/nonterminal guards;361 tests resumed/expired execution.
This is a concrete control-boundary change, not another run seeking favorable A/A.

The new prospective inactive gate is CAUSAL OPERATION EQUIVALENCE, not a claim
that independently scheduled timed searches always produce identical plans.
It requires the frozen source/compiled/real-call evidence from359--361 and absence
of treatment consumption in an entirely5000ms match. Equal starting normalized
process/session state and the SAME exogenous clock/I/O/allocation/worker history
yield the same inactive operations, submission and persistent updates. Equal-length
control values avoid environment-layout changes; the model excludes arbitrary OS
side channels and does not assert equal worker schedules on independent runs.

This changes the qualification protocol prospectively; it does NOT change357's
verdict or AGENTS' demand for an unchanged canonical5000 policy. Actual independent
5000 A/B and A/A scores/plans/state/ledger/road differences MUST all remain in
tables and downside/stratum checks. They are never relabeled causal improvements,
discarded as noise, or repaired by extra repeats. A source/runtime observation
contradicting blindness, a policy change, or an unexplained treatment consumer
blocks362 as inconclusive. Short days following different enabled histories are
not claimed cross-history equal; their own checkpoint/certificate remains binding.

Frozen357 library differs from canonical source only by appended resource function
and its header declarations (normalizing line endings); unchanged original APIs
and all other library source/header bytes under that normalization are required.
Host differs by the established process-RPC portability adapter plus registered
resource/control integration. No different low/high solver or test-only planner.
Source relation and full production integration still require explicit verification
before any product commit, not inferred from same-binary comparisons.

## Fresh frozen stages, no source or parameter tuning

Use the same validated fixture generator/grid responsibilities as357, with new
seed pool202609093620000, excluding existing experiment gameplay identities.
No opened or still-sealed357 identity is reused. Sealed setups stay local until
the preceding complete stage qualifies; upload only development initially.

- DEV36fixtures:6families x3fuels x2maps8/32, balanced5/10/15s windows,
  4/5/10days,8/9/10teams,fixed/native roles,roaded/roadless;144atomic sides.
- Holdout54fresh fixtures:full family/fuel/window coverage and alternating
  map/role/horizon;216atomic sides.
- Protected108fresh full family/fuel/map/window grid;432atomic sides.

Each fixture has two counterbalanced parent/candidate repeats. Freeze manifest,
binary/library/source/control evidence, runner, comparator, summary and gates
before observation. Existing authorized VM only. Full real-window process-RPC
daily policy with exact simulator AND independent validator; not official BTC.
Do not summarize partial scores. No rerun of completed or ambiguous partial sides.

## Gates and required reporting

1. Zero unexpected invalid/emergency/deadline skip/validator/transition/ledger/
   checkpoint/certificate failures, main/role cap5000 and original submission
   safety. Any violation blocks; expired361 negative stimuli are NOT allowed here.
2. Causal inactive operation-equivalence as stated above. Parent control emits
   no suffix frames;5000 and terminal days emit no new suffix work/telemetry.
   Read-only verification of fixed360/361 hashes and compiled guard proof mandatory.
3. For EACH repeat, benefit computed on LONGER-window rows ONLY must have
   nonnegative delta in EACH official tier, at least one positive component,
   wins>losses, and gross serving gain>=2*gross serving loss. Inactive positive
   differences cannot help this gate. No weighted score sum.
4. ALL rows, including5000, count toward downside: no individual lifetime/daily
   loss; serving loss bounded by max(1,parentServings/50). Each repeat's sum in
   every official component and EVERY family/fuel/map/window/role/horizon/player/
   road stratum must be nonnegative. Keep all loss tails and A/A uncertainty.
5. Repeatable causal wins:>=2DEV across>=2families;>=4holdout across>=2families
   and BOTH10/15s;>=2protected. A causal gain requires strict exact suffix takeover
   no later than first trajectory divergence, independently revalidated from the
   actual same-invocation parent state/plan/ledger. Merely faster prefix is not gain.
6. Holdout/protected require conservative exact-sign p<=0.05 on active-window
   robust intervals. A robust win counts only with a repeatable causal win;
   ALL robust active losses count. Report unqualified wins/unresolved intervals
   separately; do not add more repeats to qualify them. Keep all raw paired W/T/L,
   first tiers,gross gains/losses,state/plan/ledger/road hashes,work and strata.

Mechanism is unchanged357:after all old public nonterminal work, query-only
fuel-expenditure constrained target-marginal routes;32routes/1.25Mstates/4workers,
80ms validation reserve,one best exact safe strict single-agent takeover. Actual
fuel/state unchanged; existing joint/tanker work retained. Rollback returns its
complete incumbent. No extra duration lanes or reserve reallocation in362.

Only completeDEVpass may open this holdout once; only fullholdoutpass opens fresh
protected once. Preserve exact evidence even if rejected/inconclusive. A pass
does not itself authorize signature: integrate only the frozen mechanism, run
full unit/equivalence/protected checks and genuine authenticated BTC target-host
hard/3bots/public10000ms lifecycle/latency/safety gates. No production commit
until those pass. Never claim mathematical optimality or championship certainty.
