# 352: portable full-policy loss replication, not a score promotion

Registered2026-09-08 BEFORE adapter implementation or new measurement, following
the user's VM request.351 closed the certificate-to-selection consumer-defect
hypothesis.350 stays rejected; no holdout is opened or used.

Question: do the two repeatable350 DEVELOPMENT losses and its nonrepeatable
daily-tier loss persist on the authorized independent Linux host while retaining
the complete production daily policy? Include ALL three fixed cases:
2026090835090164,2026090835090213,2026090835090127. Parent is canonicalc76a8ea;
challenger is exact isolated350. Four sides per fixture: parentA,candidateA,
candidateB,parentB. No additional repeats selected from the outcomes. Expected
12 atomic sides,96 accepted days,84 transitions,3 fixture markers,1 completion.

No designed functionality is removed, disabled, deferred or reduced. Nothing is
deleted; production files and every frozen350 file stay untouched. In isolated
diagnostic builds only, replace WinHTTP byte I/O by synchronous process JSON RPC.
Keep the EXACT original source range from transient_http_status through run_http,
including role selection, real wall-clock windows, poll delays, main5000ms,
virtual/checkpoint/authoritative state, accepted258 continuation, exact validators,
ACK, resends, recovery, precompute, proof and resume. Supply a portable environment
copy helper; it must not print or persist credentials. Only a fixed SYNTHETIC
noncredential is admitted by the RPC client. No connection to BTC is made.

Broker delegates to unchanged ProtectedCase/traffic_for plus the exact dual-engine
bridge from341. Source-extraction identity tests and synthetic RPC contracts must
pass before gameplay. Freeze source archives, generated adapter, broker, helper
imports, tests, exact three public fixtures and Linux binaries before measured
cases. Both sides compile on the same host/toolchain; run sequentially. Preserve
all replay/request/ACK/validator/work/plan-state-ledger data and original windows.
The authoritative synthetic clock still advances after each full daySeconds,
not on receipt of a plan. Thus post-ACK work is retained; no fake accelerated clock.

This tests policy execution without actual WinHTTP networking. It cannot establish
Windows byte equivalence under timed search, real BTC network performance, or
promotion. A VM reduces interference from the desktop, but is NOT noise-free.
Report fixed-label paired W/T/L, first tiers, exact per-case deltas, A/A and B/B
repeatability, first trajectory divergence and activation before it. Even if all
losses disappear,350 remains rejected and these are diagnostics only. Stable
losses justify deeper same-state attribution, not a fixture-specific dispatcher.
Missing protocol coverage or safety failure invalidates this operational run;
never silently substitute sandbox/replay-solve or discard failed cases.

Use only existing authorized VM udon-f0-240-0829 in us-east1-b, project
project-10f9c8e1-a791-46a2-920. No creation/deletion, unrelated VM, RAM-floor guard,
production build/commit or additional monitor. If capacity blocks restart,
preserve the exact external blocker and finish portable preparation without
claiming any remote result. Only resume fully validated atomic sides; ambiguous
partial evidence is preserved and blocks blind repeat.
