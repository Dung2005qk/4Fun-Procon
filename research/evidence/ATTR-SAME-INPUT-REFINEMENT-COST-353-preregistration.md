# 353: same-input protected-refinement cost attribution

Registered2026-09-08 before implementation or measurement.352 is complete.
Question: does164's reduced refinement survive SAME input and SAME budget, or is
the observed61ms smaller budget sufficient to explain it? This is a direct phase
probe, NOT an accelerated match, HTTP replacement, performance verdict or SCORE.

Use352 DEV164 day1 parentA decision's config/state/ledger/main plan. Require all
four352 day1 main inputs/plans equal before freezing this one snapshot. Link a
small probe against the exact already-frozen352 parent and candidate static
libraries on the existing authorized VM. Do not rebuild or edit either library.
Use production enable flags and unchanged wait-detour THEN midday chain functions,
one shared deadline, exact simulation/independent validation and protected
transition/ledger/score checks. No function removed/disabled/reduced/deferred;
nothing deleted. No product changes, extra RAM guard, new VM or holdout.

Freeze snapshot, probe source, linked binaries, libraries and this contract before
measurement. Two observed extreme budgets1593 and1654ms, each four balanced
sides parentA/candidateA/candidateB/parentB. Then four balanced fixed-point
controls at10000ms with the SAME operation caps/functions. The latter are direct
phase diagnostics ONLY, not permission to lengthen the5000ms production main.
Expected12 calls, no selected favorable repeats. Stop/report operational failure,
do not silently rerun. A completed result must have matching frozen identities.

Report all scores/plans, wait and midday time, deadline flags, global/target
work/acceptances, exact validity and same-input immutability. If10000ms still
cuts off, classify it incomplete; never silently enlarge the cap. If complete
controls differ, prioritize semantic/state divergence before any speed claim.
If equal short budgets retain a consistent library deficit but complete controls
agree, inspect shared sparse-frontier cost/code-generation. If only varying the
budget causes the deficit, inspect upstream resource allocation. If neither
explains it consistently, report inconclusive and do not tune the fixture.

No general promotion follows even from a consistent result. A new mechanism needs
separate source/equivalence evidence and fresh registered score/holdout gates.
Do not repeat rejected240 ordering or344 post-W1 relocation as a new fix.

## Pre-measurement operational correction2026-09-09

The first invocation failed at parse_match_config before any simulation or solver
call: audit.config contains counts only (`agentCount` unsupported). There are
zero atomic results. Preserve original artifacts353/execution and raw stderr;
this is not gameplay evidence. Use the exact full setup from frozen352 input
F87841F53A693CAD141DC0B434201F8145CAF6180A0A897954C61A7E7F6D15C4, verify audit counts,
then preserve all original state/ledger/plan fields. Corrected artifacts live
under353/validated and evidence353-validated; freeze again before all12 original
calls. No code/library/budget/mechanism changes or favorable-case selection.
