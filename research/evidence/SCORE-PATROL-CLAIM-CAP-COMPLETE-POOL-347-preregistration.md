# 347: per-Patrol claim-cap intersection with complete-pool pricing

Registered2026-09-08 before fresh inputs or candidate source changes. Parent is
canonical c76a8ea/accepted258. Candidate is isolated complete-pool344 mechanism
plus ONE sound bound correction in FastViabilityAnalyzer. Rejected344 remains
rejected and its holdout remains sealed. No consumed holdout tunes this mechanism.
Fresh counterexample and exact feasibility are345/346 DEVELOPMENT evidence.

Preservation: no designed functionality removed, disabled, deferred or reduced.
Nothing deleted. Original role/search/F0/W1/current-floor/confidence/strict
dominance/ACK/suffix/validator/transport functionality remains active. No cap
increase, forced future action, extra oracle, branch by fixture, or RAM floor.

## Bound and intended consumers

Let D be remaining days, S the number of spots, and f_i current Patrol fuel.
Without any tanker, a Patrol can claim at most D+f_i: at most one initial-cell
claim per day, and every other claim requires a move costing at least one fuel.
Independently, the once-per-Patrol-per-spot-per-day rule bounds claims by D*S.
Therefore sum_i min(D+f_i,D*S) is a valid global claim upper, before applying
the existing total-stock and franchise bounds. Existing unreachable-Patrol
exclusion is retained. All arithmetic uses int64 intermediates. With a tanker,
the existing fuel-unbounded path is unchanged. Empty stock/unreachable/expired
cases remain sound. This is not a tighter bound obtained from a partial search.

Replace only each noTankerClaimCapacity addend with its intersection; do not
replace the global bound, pessimistic/conditional/frontier consumers or any
search responsibility. candidate_valid_upper_bound continues to derive exact
post-current-action state/ledger. All existing consumers receive the same bound
object. Existing strict certified dominance prunes BEFORE unchanged current-floor
selection. No176 equality-plus-upper exception. Complete-pool pricing remains
unchanged344, including all original outcomes, resource limits, deadlines,
same-invocation no-gain pool/selection equality and exact witness/ACK consumers.
The combination is score-affecting and needs fresh qualification, not a free fix.

Preflight: prove the inequality; check ALL96roots37unique346requests against exact
optima without new oracle calls; require soundness and strict capability across
two roots/two families. Direct C++ tests must cover active tightening, unequal
agent fuel, zero fuel/spot start, one spot, terminal horizon, tanker exemption,
expired coarse bound, input immutability, and existing strict dominance versus
unchanged current-floor selection. Run all original protected/unit contracts.

## Fresh qualification and immutable gate

Reuse344's fresh fixture generators and frozen repeated comparison protocol:
24development,54sealed,108protected; two fixed repetitions per binary, cyclic
parentA/candidateA/candidateB/parentB order. New seed ranges and reject gameplay
identity collisions with all consumed inputs plus unopened344 precomputed gameplay
identity hashes only (never inspect sealed344 configurations or scores). Freeze all inputs BEFORE source.
Preserve 8/9/10teams, variable steps/horizons, fuel/stock/role/map/traffic and
5/10/15second public-window protected strata. Canonical main stays5000ms.
No fixed available-RAM guard. Only real operational/validation failures stop.

Acceptance is exactly344's preregistered score/breadth/downside/control policy:
each narrow repeat positive servings net, more wins than losses, gains>=2losses,
zero lifetime/daily loss, worst servings loss<=1, nonnegative family nets;
>=4development or8sealed distinct wins in BOTH repeats across>=2/3families,
with certified pricing gain no later than first trajectory divergence. Bound-only
or unsupported timing wins alone cannot satisfy mechanism-breadth qualification.
All no-gain/audit/validity/deadline/resource gates retained. No weighted sum.
Protected per-repeat and per-stratum component nonregression, positive overall
net, W>L, gains>=2losses, per-pair serving loss<=max(1,floor(parent*0.02)),
robust cross-repeat exact one-sided sign p<=0.05 and>=2repeatable causal supported
pricing wins. Unresolved intervals reported, never relabeled or noise-subtracted.
Use the frozen344 functions unchanged, with the already-proven roadless transport
normalization and JSON-normalized report comparison from the outset.

Only full development pass opens this sealed split once; only sealed/protected
and exact integration/protected lane champion plus genuine BTC target-host gates
authorize product promotion/commit/signature. No partial scores, optional repeats,
old holdout rescue, or metric changes. Preserve existing data on failures.
VM remains authorized for useful actual research/holdout; never waste build/test
round trips. Current WinHTTP target-host timed execution remains local unless
an equivalent remote runtime path is established before timing inputs freeze.
