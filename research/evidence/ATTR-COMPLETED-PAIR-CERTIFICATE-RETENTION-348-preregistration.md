# 348 — complete pair certificate lifetime under later exhaustion

Registered2026-09-08 before observational changes or measurements. Attribution
only; no production edit, played-score claim, sealed input or promotion authority.
No designed function removed, disabled, deferred or reduced. Nothing deleted.

Source in isolated347 horizon_pricing.cpp constructs and dual-validates a COMPLETE
remaining-horizon witness for each unordered physical pair, compares its exact
ledger with the DP optimum, and keeps a strict best in a local optional. Later
pair exhaustion unwinds that optional; the outer catch returns no improvement.
The complete earlier certificate is logically independent of completion of later
pairs because all pairs start from the same original root and fixed trajectories.
Whether a usable strict certificate is actually lost is not yet measured.

Use ALL24 requests in the previously opened339 DEVELOPMENT attribution manifest
(selected original338development W1 day1 suffixes), with exact hashes verified.
This is diagnosis, not fresh qualification. No347sealed/protected inputs.
For each request run the unchanged default resource caps and transition caps
65536,131072,196608. Keep100ms wall ceiling and other five limits unchanged.
These are predefined fault-injection levels, not tuned time/cap recommendations.

Observation moves the ownership of the existing local best to the Pricing object
solely to read it after unwinding. Record completed pair count and work at its
last strict update; do not alter return value, branch ordering, admission, caps,
resource charges, or apply observed plans to an HTTP session. Diagnostics are not
latency measurements. Copy/serialize retained witness only outside measured work.
Expired and malformed inputs retain current behavior. InvalidWitness must never
authorize publication. Original input remains unchanged. All pairs still run.

Freeze all source/probe/library/input hashes before execution. Every returned and
observed witness must pass exact simulator plus independent validator, ledger
score and unchanged non-pair trajectory checks. A partial pair is never a witness.
Test complete no-exhaustion score/plan equality against frozen340, expired/corrupt
controls, each resource ceiling and deterministic later-pair exhaustion controls.

Gate for separate fresh SCORE design: discarded fully validated strict witness
on >=4distinct DEVELOPMENT roots in >=2families; zero validation, bound, original
mutation or score reconstruction failure. Report all96queries and all families,
including no-gain/full-completion cases. A negative result closes this hypothesis.
A positive result proves capability loss only, not broad strength, closed-loop
monotonicity or competition readiness. Any production successor needs genuinely
fresh development/holdout/protected qualification and must preserve every search
consumer, current floor, state/ledger/traffic contract and deadline.
