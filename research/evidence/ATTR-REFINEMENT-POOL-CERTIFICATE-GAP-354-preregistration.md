# 354: fixed two-plan certificate and route-pool attribution

Registered2026-09-09 BEFORE implementation.353 exposes parent65 at1654ms vs64
with ample time. Use ONLY its exact parentA snapshot and plans from those two
completed rows. No new match, no SCORE, no replay/holdout reuse for promotion.

One fixed exact comparison: all256 agent-wise mixtures of the two8-agent plans,
including endpoints. For every mixture report exact validity, official score,
terminal/fuel/road/ledger dominance relative to BOTH original55 and endpoint64,
plus which agent components changed. This distinguishes a remaining one-agent
improvement, cooperative improvement and a stricter intermediate-certificate
barrier. Do not remove the certificate to manufacture a gain.

Additionally enumerate the unchanged target-terminal pool for ALL8patrols from
the original day1 state, with the same minimum5spots/32routes/1250000settled cap,
no preferred brands (all six already in incumbent), terminal from the original
protected main plan. Reuse the exact352 parent library. One30second shared outer
guard for these8queries; preserve per-query completeness/settled counts, every
returned route and membership of64/65 endpoint components. A missing route does
not prove heuristic eviction until its discovery is also demonstrated. No cap
expansion or rerun if incomplete. Validate each returned route substitution in
endpoint64; record score and the same exact certificate, never apply plans live.

Freeze inputs/probe/binary/library before this single run. Preserve full outputs
and verify recomputed score/certificates. Stop on operational/validator mismatch;
do not alter outcome set after observing results. Product/library source and all
functionality stay unchanged; no deletion, disabling, deferral, RAM guard or VM
creation. Canonical main5000ms remains unchanged. No350 revival or holdout opens.

Related257/258 solved TERMINAL team-marginal retention; here NONTERMINAL fuel,
terminal, road and future ledger constraints remain binding.215 already supplies
target-terminal pools. A new mechanism is permitted only after this distinction
is resolved; it still requires fresh source/equivalence/score/holdout/target-host
gates within the user's remaining day. Never call this proof of global optimality.
