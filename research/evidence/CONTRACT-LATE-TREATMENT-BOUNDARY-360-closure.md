# 360 complete: late control boundary passes the finite compiled contract

2026-09-09. No product source/binary change, deletion, functionality reduction,
budget change, commit or holdout opening. Reused the exact357 library and headers;
compiled only the isolated host. Both experimental consumers remain wired.

Four synthetic VM full-lifecycle matches completed:16 ACKs,12 reconciled
transitions,zero stderr/failure. Inactive5/5/5/5-second cases read the control
zero times for BOTH0 and1. Mixed5/10/5/10 cases read it exactly once, on day2;
no startup/role/day1/day3/terminal-day4 reads. Candidate day2 entered with3queries,
no takeover, no deadline rollback and no failure. This is no-gain lifecycle
coverage, not score improvement or evidence of independent-clock equality.

Frozen execution47488178393C4578A1D28795CA2EFDC9329F0B6BE96A75967774446F28FC68F9;
resultF61EB2B622550443556D56B1975F011C46A9385ED9F1C1003889E1C608449808;
downloaded archiveB4C42C4AC44CC9AA82083DD03495A29B318350A5C43E3B294517E2FC6611A3D3.
VM --check and independent local verifier both pass. Local verification checks
all281 frozen357 dependencies,360 staged/built identities and26 contract files.
Source reversal reconstructs the ENTIRE frozen357 host exactly, proving the
edit is confined to the registered control replacement, not planner/ACK changes.

## Machine boundary

Plain D3684DC3F4ADB5E6E74F5E75412CCB76E681D21F760B6BC0FA61EDC25A029678;
probe3C544650192D22CBA22300C8F728336EF026A5E5F86A37E514F1AD6E04559ED7;
shared host object170D101E00FD7DE983751815BE4FF5DBAFB0DBF03E3F767ED90C3F91CB0F667B.
The linked run_http control reader is at relative offset0x567a in BOTH links:
plain0x3f78a and probe0x3f8ba. The other getenv call is the normal token reader;
the probe records only the non-secret treatment name, never environment values.

The plain public-authorized branch0x3ee72->0x3f75c and nonterminal
fall-through0x3f77d->0x3f783 each dominate that reader in normal control flow.
The original public refinement returns through0x3fea1->0x3f765 before the
terminal check. The local verifier checks both binaries' normal-flow graphs,
with known C++/libc noreturn throw/abort calls terminating paths; no unresolved
indirect jump exists in the inspected function. Its initial conservative model
incorrectly allowed throw routines to return, so the graph assertion failed;
the corrected parser/noreturn model passes without changing any executable or
measured evidence. This is not a whole-program exception or OS-level proof.

## Persistent-state and scope

Full replay-check loads four accepted days in all four runs. Source
load_replay_resume ignores resource_marginal diagnostic records and reconstructs
state only from the original decisions/checkpoint/actions/ACK/session checkpoints.
The same late guard is executed on every new authoritative day; no treatment
value is saved in session state. Thus a short day after a long day does not read
the treatment again. Different previously accepted plans MAY yield different
persistent state: absence of a fresh read is not cross-history score equality.
Conditional noninterference requires equal normalized starting state and equal
exogenous clocks/I/O/allocation/worker schedule, not just equal map and budget.

Removing the interposer preserves the guarded read in the same compiled host
object; it does not prove a future hard-enabled/recompiled product equivalent.
That integration still needs its own frozen equivalence/protected/BTC gates.

Decision: PASS scoped360 contract, not promotion.357 stays inconclusive and its
sealed54/protected108 remain unopened. No automatic SCORE successor. Remaining
finite boundary coverage is live restart from an acknowledged mixed-window prefix
and an already-expired authoritative window, including exact no-replay/no-duplicate
ACK behavior. Register361 to exercise those existing bytes without source change,
rebuild, search-policy reduction or any score qualification. No measured job remains
from360. Canonical c76a8ea/accepted258 is unchanged.
