# 356 resource-conditioned marginal capability

Registered2026-09-09 before implementation. No product/source/library edits,
function reduction/disabling/deferment/deletion, holdout, RAM floor or new VM.

355 proves a safe emitted option is excluded by route-local retention; simply
ranking team servings still admits many fuel-infeasible candidates ahead of it.
Test a bounded general mechanism through the unchanged original library:

1. Freeze the complete parent64 plan from354. Obtain exact claim masks from its
   simulator/independent-validator-agreed trace. For each patrol form team claims
   excluding that patrol and lifetime brands, using existing marginal context.
2. Query its protected terminal with a *query-only* state copy whose patrol fuel
   is initial fuel minus protected final fuel. This is the no-refuel expenditure
   subspace compatible with the certificate, not a changed actual fuel state or
   restriction of any existing planner. Negative bound has no such route. Existing
   canonical/refueling search remains intact; this addition is not exhaustive.
3. Same original minimum-spots formula,32 routes,1,250,000 states,4 workers,
   one shared 5000ms diagnostic suffix budget with80ms validation reserve.
   No changed ordering, larger cap, repeated main solve or parameter sweep.
4. Evaluate retained marginal routes as one-agent substitutions into the actual
   original state/complete parent. Both exact engines plus original full
   transition/ledger certificate and strict official lexicographic score decide.
   Select at most one best strict takeover. Expiry/failure returns parent exactly.
5. Exactly one expired-deadline control and one bounded capability call, all8
   patrols included. Record query work, every retained candidate's validity,
   certificate, score and fuel, complete parent/output, elapsed and rollback.
   No favorable repeats. Assert actual input unmodified and route cost <= bound.

Positive means a completed, dual-valid strict safe improvement inside this
diagnostic budget, with expired call byte-identical. It only permits a separate
fresh SCORE registration and full policy integration in the public continuation
after the complete canonical prefix; it does not grant product/promotion authority.
Negative closes this mechanism on current evidence without a cap ladder.

303 is not reopened as a speed optimization: this query changes resource/stock
conditioning to retain additional certified options absent from the old pool.
The actual main5000 checkpoint and all original refinement phases remain intact.
Same authorized VM/root352; reuse original frozen library without rebuilding it.
