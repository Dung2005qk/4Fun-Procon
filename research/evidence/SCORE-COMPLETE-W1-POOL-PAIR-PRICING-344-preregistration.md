# 344 — complete original W1 pool, then bounded joint pricing

Registered 2026-09-07 before new inputs or candidate source. Parent c76a8ea;
canonical BTC SHA256 F97F168FE76FEF1B6226D2C2CFDAF954F116D39F427B6F157594151C461E1275.
This is a new SCORE experiment, not a revision of rejected341's verdict.

## Preservation and exact scope

1. Does this remove, disable, defer or reduce designed functionality? No.
   Original roles/search/F0/W1, complete monotone floors, every required scenario,
   current-day admission, dual validation, protected refinement, actual ACK,
   cached full suffixes, post-ACK work and transport reserves remain active.
2. Anything deleted? No files or capability. The inline research pricing call is
   relocated after the original complete pool; its only production caller is
   solve_day. A shared canonical selection helper replaces duplicate selection
   plumbing only after direct equivalence tests, preserving order/tie semantics.

Start from canonical source plus frozen341 resource/pricing capability, isolated
under artifacts/research/344/source. No production edits. Resource model, all
unordered physical pairs, original-root cache, stock-relaxed sound bound,
terminal mask quotient, all-pair rollback, six resource limits and100ms maximum
per witness remain identical. No threshold or dispatcher by seed/family/map ID.

Complete every ORIGINAL W1 repair call and collect the entire certified pool
before refinement. Finalize/choose its original winner using the same comparator,
dominance and current-floor logic. Retain all original candidates (including ones
currently dominated), so better exact witnesses can still change final selection.
Price each complete non-lower-bound-only outcome in original pool/scenario order
under the ORIGINAL shared certification deadline, leaving validation/network
reserves intact. No rerun of main or W1. Only a complete strict exact witness can
replace an outcome; no-gain/unsupported/exhausted calls leave that outcome intact.
After any gains, canonical finalization/selection runs on the improved pool.
Otherwise the original finalized pool and selection are retained exactly.

Audit same-invocation base pool size, base/final candidate identity, original and
priced outcome scores, exact current-plan identity and witness replacement count.
Tests compare full no-gain pool/witness/selected plan bytes, not merely telemetry
booleans. Audit must expose unsupported/exhausted/completed work and all failures.
Original metadata, scenario identity/weights, upper bounds and current simulations
are immutable through pricing. Final selected witness continues through canonical
record_submitted and full certifiedSuffix admission (333); no forced next action.

Extra selection/audit/pricing time may still affect later refinement and post-ACK
work. It is NOT refunded or called operation-equivalent. Independent runs may
differ before pricing (342). Report these differences; do not erase them as noise,
force roles, remove current floor or claim a counterfactual whole-policy proof.

## Fresh inputs and repeated execution

Freeze all three splits before source:24 small roadless development fixtures,
54 sealed small roadless fixtures,108 protected fixtures. Existing immutable
332 fixture generators/official validators are retained;6 small families and
6 protected traffic families. Protected covers map8/32, low/default/high fuel,
fixed/native roles,4/5/10days,8/9/10teams,5/10/15second public windows and exact
own plus aggregate external two-day road history. Canonical main cap5000ms.
Reject duplicate gameplay identities against consumed inputs without inspecting
scores. Identity-only collisions advance the deterministic generator cursor;
record every skipped seed. No gameplay-conditioned sample selection.

Every fixture runs parentA,candidateA,candidateB,parentB with cyclic rotation by
fixture index. Thus two preregistered actual closed-loop pairs per fixture and
separate within-binary A/A controls, never a median picked after observation.
Development96 sides, holdout216 sides, protected432 sides. Only complete phases
are summarized. Verify hashes, exact artifacts, ACKs, transitions, validity and
no ambiguous partial side before resume; never replay an accepted partial day.
RAM floor1024MiB before each side. No concurrent local compute. One10minute
monitor initially; adjust only to measured duration at a meaningful transition.

## Gates frozen before inputs

All phases: zero invalid/emergency/simulator-validator/checkpoint/pricing failure,
zero hard-deadline violation; six pricing bounds; zero same-invocation no-gain
pool/selection mutation. Report every paired official lexicographic W/T/L,
first differing tier, all component deltas and gain/loss tails, roles/plans/
state/ledger/road identity, pricing-before-divergence and every registered stratum.
No weighted sum of official tiers. Timing is synthetic diagnostic evidence only.

Development and sealed: each repeat separately must have positive servings net,
more wins than losses, gains at least twice losses, no loss of lifetime/daily
distinct, worst per-pair servings loss at most1, and nonnegative family nets.
Require at least4 development /8 sealed DISTINCT fixtures won in BOTH repeats,
spanning at least2 /3 families. Those repeatable wins must have certified pricing
gain by the first plan divergence in both repetitions. These narrow gates retain
the previously preregistered small downside tolerance, not absolute domination.

Protected: report both fixed-label A/B pairs and all four cross-repeat comparisons
per fixture, plus parent/candidate A/A. A robust win means the lexicographically
worse candidate repeat strictly beats the better parent repeat; robust loss means
the better candidate repeat loses to the worse parent repeat. Mixed intervals are
reported as unresolved, NEVER silently ties or wins. Require positive component
nets and more wins than losses in EACH fixed-label repeat, nonnegative component
nets for each preregistered family/fuel/map/role/horizon/player/window/road stratum,
and a one-sided exact sign probability <=0.05 on robust wins versus robust losses
(one observation per independent fixture; unresolved fixtures excluded only from
this sign calculation and all still reported). Repeatable beneficial supported
pricing must span at least2 fresh protected fixtures; mere runtime wins in
unsupported domains cannot qualify the mechanism.

No observed lifetime/daily-distinct loss may be accepted as scheduler noise.
Servings-only downside may be accepted only if each fixed-label pair loses no
more than max(1,floor(parent servings*0.02)), total gains are at least twice total
losses in each repeat, and no public stratum regresses. This percentage is a new
pre-input global tolerance, not fitted to a particular consumed case. Any mixed
or failed statistical/safety/breadth gate blocks promotion; controls describe
uncertainty and do not subtract away losses. Do not add more repeats opportunistically.

Only development pass permits opening this sealed split once. Only full sealed
and protected pass permits canonical integration, exact caller/consumer gates,
all protected lane champions and genuine authenticated BTC target-host lifecycle/
latency/safety validation before an improvement commit or competition signature.
Integration that changes frozen mechanism needs a new experiment. Failure closes
344 with exact evidence; no automatic gate change or consumed-holdout tuning.
