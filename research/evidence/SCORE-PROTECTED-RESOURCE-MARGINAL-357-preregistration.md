# 357 protected resource-conditioned marginal continuation

Registered2026-09-09 before candidate source changes or fresh fixture measurement.
Parent:canonicalc76a8ea/accepted258.355 proved safe emitted route eviction;356
recovered64->65 using the existing API in748.911ms diagnostic time with unchanged
actual state and exact full certificate. Those are mechanism tests, not promotion.

## One mechanism and preservation

After ALL canonical public nonterminal refinement work returns without deadline,
freeze its complete plan/simulation/ledger. Only then use remaining authoritative
public time for ONE bounded resource-conditioned marginal pass. Main solve,
role selection, complete5000 checkpoint, existing wait/global/target/pair work,
terminal258, ACK/proof and consumer lifecycle remain unchanged. No main rerun.

For each patrol query its already-protected terminal using an ephemeral DayState
copy with fuel = actual initial fuel minus protected final fuel. Negative bounds
have no no-refuel query. Same original min-spots formula,32routes,1,250,000-state
cap,4workers and80ms validation reserve. Existing marginal context ranks exact
team stock claims excluding that patrol. No seed/map/opponent dispatcher, new
objective, cap ladder or fuel mutation. This adds a conservative no-refuel
candidate subspace; existing tanker/refueling paths remain fully active.

Evaluate each retained marginal route as a single-agent substitution into the
actual parent state and complete plan. Both exact engines, original transition
dominance (terminal, fuel, footprint), ledger component dominance and strict
official lexicographic score are mandatory. At most one best strict takeover.
Any deadline, failed worker or certificate/engine failure returns complete parent
byte-identically. No parent functionality is removed, disabled, reduced, deferred
or deleted; no equivalent needs deletion. New API is shared, not a second solver.

Research A/B uses one candidate binary with a process-scoped environment flag,
read once at startup and consulted ONLY in the authorized public suffix. Flag
off runs the canonical daily policy. No secrets in that flag. Production integration
may hard-enable this exact tested suffix only after all gates. No research flag
is silently enabled in current production.

## Frozen stages

Before isolated source creation freeze fresh fixture identities, comparator,
strata and these gates. Existing opened seeds and unopened holdout identity hashes
are excluded without inspecting unopened setups or scores.

- Mechanism/unit preflight:356 frozen witness; expired/terminal/unsupported/no-gain
  preservation, input immutability, bounded work and exact certificate; no score tuning.
- Development:36 fresh broad fixtures covering6traffic families x3fuel x2map8/32,
  balanced5000/10000/15000 windows,4/5/10days,8/9/10teams, fixed/native roles,
  roaded/roadless. Two balanced parent/candidate repeats,144atomic sides.
- Holdout:54 fresh fixtures,6families x3fuel x3windows with map/role/horizon
  alternation. Sealed until complete development qualifies.216atomic sides.
- Protected:108 fresh full family/fuel/map/window grid, fixed/native roles,
 4/5/10days and8/9/10teams.432atomic sides, never open before holdout qualifies.

Full real-window daily policy on existing authorized VM via validated352
process-RPC adapter, exact traffic/ACK bridge and fresh fixtures. This is a local
simulator hosted on VM, NOT official BTC. Same-binary paired repeats preserve
source/layout on both sides; no host comparison or desktop latency claim.

## Decision gates frozen before observation

All stages:zero invalid/emergency, exact validator/transition/ledger/checkpoint
failures and authoritative deadline violations. At5000ms the new suffix cannot
activate; require source/runtime operation equivalence and exact score/plan/state/
ledger pairing. If baseline A/A timing noise itself prevents exact pairing,
classify the stage inconclusive, never silently relax equivalence or rerun.

Each repeat must have nonnegative aggregate delta in EACH official tier, at least
one positive delta, wins>losses, gross serving gain>=2*gross serving loss; no
individual lifetime/daily loss, serving loss bounded by max(1,parentServings/50).
All public strata (family,fuel,map,window,role,horizon,players,roaded/roadless)
must have nonnegative component sums in each repeat. No weighted tier combination.

Development requires >=2 repeatable causal winning fixtures across >=2families.
Holdout requires >=4 across >=2families and both longer windows; protected >=2
fresh repeatable causal winners. For holdout/protected require one-sided exact
sign-test p<=0.05 on robust wins/losses (candidate minimum vs parent maximum).
Report all unresolved intervals, same-binary controls, paired W/T/L, first-tier
differences, gross tails and every stratum, even if gate fails.

Causal gain requires a logged strict suffix takeover no later than first actual
trajectory divergence, exact same-invocation parent/output certificate, and no
claimed win caused solely by startup/cutoff noise. Whole policy differences still
count in paired downside; certificate safety does not exempt outcome regression.

After all frozen stages pass:integrate only exact frozen mechanism, full tests/
equivalence, then true authenticated BTC hard/3bots/10000ms target-host lifecycle,
latency and safety. Only then product commit/signature. No microcommits, RAM floor,
new/deleted VM or consumed holdout tuning. Failure closes357 without weakening
gates; current canonical remains the best qualified competition artifact.
