# 354 complete: safe one-agent option missing from retained target pool

2026-09-09. All256 fixed two-plan mixtures and8 target queries completed;
local frozen check matches raw data and input. Zero validator mismatch.
No product/function/source/library/cap change or deletion.

The65 endpoint strictly improves64 with EXACT equal final agents (roles,
positions and fuel), equal road footprint and nondecreasing ledger. Mask32 alone,
changing only zero-based agent5, achieves65 under the SAME certificate relative
to64. Thus this counterexample is neither a necessary multi-agent valley nor an
intermediate fuel-certificate barrier.128 mixture masks improve, but these are
combinations of only two existing endpoints, NOT128 independent discoveries.

All8 fixed queries settle exactly1250000 states, report searchComplete=false
(operation cap), and retain32 routes. No returned route substitution safely
improves64. The65 component for agent5 is absent; so is an equivalent-score
component for agent4. Missing from retained pool is established. Whether the
useful route was discovered and discarded, or absent from that capped search,
is not yet established and must not be asserted from membership alone.

355 will observe all emit-eligible choices in exactly the original agent5 target
query at the same cap, with an unobserved fixed-operation output-equivalence
control. No larger search, timed ladder, fallback disabling or new match needed.
Related257/258 provides a terminal-retention precedent, not authorization to
enable it nonterminally without full future-state certificates and fresh gates.

Result SHA256:EB9BB7AFB807D95E4ED3C5136B6458EE45AC0807A47EBE90D3C34304B1FA9747
Manifest SHA256:5472F5831AB6060D1064FD76C7B0BE16270FFF935ECCE4F04F33641DE4E8F97C
Raw/atomic/execution hashes and full plans are preserved under evidence354 and
artifacts/research/354.350 remains rejected; canonicalc76a8ea/accepted258 unchanged.
