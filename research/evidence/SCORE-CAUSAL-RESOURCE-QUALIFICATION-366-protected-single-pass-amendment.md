# 366 protected: user-authorized single-pass amendment

Recorded 2026-09-10 Asia/Saigon before protected staging, execution or observation.
Direct user instruction: "protected co the chay 1 luot thoi, khong can chay 2 luot".
This explicitly supersedes the TWO-repeat requirement only for the unopened366
protected stage. It does not retrospectively alter DEV or the running holdout.
The original preregistration, frozen controller/classifier, execution manifest,
all evidence and362's original rejected verdict must remain byte-identical.

Preservation answers: no designed PRODUCT functionality is removed, disabled,
deferred or reduced; nothing is deleted. The user explicitly reduces repeated
evaluation, with the inferential limitation below. No optimizer, source, binary,
budget, transport reserve, certificate or safety-policy change is authorized.

## Fixed scope and execution

- Finish and verify the existing54-fixture TWO-repeat holdout under its ORIGINAL
  frozen gate. Failure closes366; this amendment cannot rescue a failed holdout.
- Only after its complete pass, open the exact original108-fixture protected
  setup, SHA256 02E4C55AD2C8C506239E0B947BB5E2DA955D329C8BADC2B5979135F93A52DEB7.
  No seeds, configurations or public strata are removed or substituted.
- Run exactly parentA and candidateA once per fixture. Retain their relative
  order from each frozen run_order; do not always run parent first. Do not run
  parentB/candidateB or select a repetition based on outcomes.
- Same frozen360 binary, treatment controls, full runtime, exact simulator,
  independent validator and causal certificates. No shortened days or HTTP
  lifecycle. Expected108fixture markers216atomic results216certificates,
  1368ACK1152transitions and one completion marker. Minimum wire13680seconds,
  approximately3.8--4hours with overhead, not a completion-time guarantee.
- Use distinct protected-single output/runner companions and a separately
  hash-frozen measurement adapter. Never edit the running366 controller or
  invoke its original four-side protected run. Before opening protected, prepare
  and test this mechanical adapter using synthetic inputs only, including
  missing/duplicate sides, unsafe certificates, failed previous gate and adverse
  score controls; freeze its source, tests and this amendment in an execution
  manifest. All inherited dependency and binary hashes must still pass.
- No partial scores, optional stopping, extra lucky repetitions or source tuning.
  Recovery retains the original no-duplicate-accepted-days/atomic ownership rule.

## Single-pass decision rule, fixed before observation

All original safety, main/role5000, independent validation, transition/ledger,
inactive control-blindness and zero-unexpected-failure obligations remain.
For all108 pairs and every public stratum, preserve official lexicographic W/T/L,
first differing tier, each component delta, gross gain/loss, worst tail, work,
deadline/certificate telemetry and paired trajectory hashes.

Apply original366 per-repeat overall/active benefit, active downside and active
stratum requirements to the one A repetition: every overall component>=0;
active components>=0 with positive benefit, wins>losses, gross serving gain at
least twice loss, every active stratum component>=0. No individual lifetime or
daily-distinct loss in any lane. Retain the original serving downside bound
max(1,parentServings/50) in ALL lanes, without any A/A-derived relief.

For inactive5s use one normalized serving delta per fixture, denominator
max(1,parentServings/50), with at least12 fixtures. Retain exact one-sided sign
noninferiority median>-1 at p<=0.05 and block a detected negative median at zero
at p<=0.05. Boundary ties are excluded; insufficient power does not pass.
No candidate suffix frames/control consumption on wholly5s matches. Report all
inactive score and trajectory differences; none is a causal mechanism gain.

Require at least2 active causal winner fixtures. A causal win requires official
paired victory plus independently validated same-invocation strict takeover
before or at first divergence. For the fresh active single-pass sign test, count
ONLY these causal wins as wins and ALL official active losses as losses; require
one-sided exact p<=0.05. Preserve noncausal/unresolved outcomes separately.
Do not label this a repeated-interval or repeatability test.

## Authority and limitations

Single-pass protected has no same-fixture A/A spread, no replicated winner test
and no nonoverlapping two-repeat intervals. These must be reported unavailable,
not fabricated by duplicating A as B, treating missing B as a tie, borrowing
another fixture's noise or calling the old classifier with fabricated rows.
Repeatability evidence comes from the full original DEV and holdout, NOT from
this protected screen. This is less repeated evidence than the original plan;
the original two-repeat protected protocol must never be reported as passed.

Acceptance, if all gates pass, is explicitly under the user-amended evidence
package: complete original DEV + complete original holdout + full108-fixture
single-pass protected + exact product integration/protected tests + genuine BTC
target-host gates. No automatic parent, commit or signature from this amendment.
No two-agent research until parent acceptance. Qualifier decision cutoff and
single40minute monitor remain unchanged.
