# 361: resumed and expired-window boundary, contract only

Registered2026-09-09 after complete360. Preservation answers:no designed function
removed/disabled/deferred/reduced;nothing deleted. No production or candidate
source change, build, budget change, score experiment, holdout or commit.

Use exact360 execution47488178393C4578A1D28795CA2EFDC9329F0B6BE96A75967774446F28FC68F9
and resultF61EB2B622550443556D56B1975F011C46A9385ED9F1C1003889E1C608449808.
Existing authorized VM only, new scoped /home/LMC/udon361-0909. Verify360 and357
dependencies before execution. No VM creation/deletion. No solver/library rebuild.

Four synthetic restart sessions: both frozen360 links (plain/probe), each with
equal-length non-secret control0/1. Use the completed360 mixed/native contract
prefix ending after day2 ACK, including following session-checkpoint artifacts
but excluding day3 state. Reconstruct an independent synthetic server's matching
state/ledger/own traffic by dual-validating BOTH accepted plans from initial state;
never send those two actions again. This is a new lifecycle contract, not recovery
or rerun of the original completed360 match. Prefix bytes are preserved exactly.

At restart deliver day3 with an intentionally expired endsAt. Expect no action POST
for that day and the unchanged production expired-day WAIT/ledger path. The
synthetic server applies the same exact independently validated WAIT once. Then
deliver a normal terminal day4 with the original10-second window and real wall
clock; require exactly one valid final action and ACK, no assignment repost and
no duplicate accepted prefix. Keep original four-worker/full-policy solver;
no fake compute clock, reduced caps, role override or altered deadline reserve.

Probe must log zero additional treatment reads across restart, expiry, terminal,
ACK and replay-check. Plain/probe use the same original host object; neither link
has a different planner implementation. Replay-check must accept all4 days and
3 transitions including the deliberately server-applied WAIT. Standard ordinary
score-run safety checker is not suitable here: expected expired skip is explicitly
the negative contract, not an allowed event in a future score qualification.

Freeze script/input hashes before execution. Preserve all requests, stdout/stderr,
prefix, resulting replay and simulator records with hashes. Never blind-rerun a
partial session. Fail stops this contract for attribution, not product downgrade.

Completion can close the finite lifecycle obligations from359; it cannot prove
independent-clock score equality, cross-history state equality or overall OS
noninterference. A prospective SCORE protocol still requires separate reasoning,
fresh unopened fixtures and unchanged official benefit/downside/safety authority.
357 verdict and its sealed54/protected108 stay unchanged/unopened.
