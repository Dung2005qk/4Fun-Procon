# 347 protected bridge binding — pre-measurement operational amendment

Registered 2026-09-08 after the first protected launch failed, BEFORE any
protected side, accepted action, replay or score was emitted or inspected.
This repairs a harness binding, not the candidate or the score gate. No designed
function is removed, disabled, deferred or reduced; nothing is deleted.

PID7104 exited at its initial config request with `invalid protected setup`.
The actual child response was `unknown bridge operation`: the common execution
manifest selected `artifacts/research/314/bridge.exe`, which only supports
fixture/step. ProtectedCase additionally needs config/roles and the exact road
footprint. Its intended bridge is the existing
`artifacts/research/341/protected-bridge.exe`, already frozen in347's1600
dependencies at SHA256 B8C26E696885FB3C9CC8F99D2F6194C0CFD62FF0229C8C9CA0D9DF862EA47EC6.
Its source hash is C4AD5C5D0D343557E9CC58BA92E220CE9B906DD49CEC5A523FF6719FE17A1875.
Both bridges call the same exact simulator and independent validator. The
protected bridge additionally validates roles/config and returns the already
computed exact road footprint. It contains no planner or oracle.

Preserve the failed .runner.stdout/.runner.stderr and empty protected directory.
The frozen atomic audit has confirmed zero results, zero fixture markers, zero
replays and no ambiguous side. The prior document's protected-start entry records
the launch attempt, not a healthy score run. This failure has no score verdict.
Original347 execution, wrappers, manifests, binaries and completed development/
sealed evidence remain byte-for-byte intact and independently recomputable.

Before recovery: validate all108 existing protected setups and fixed/mixed role
inputs with the already-frozen protected bridge, run existing transport contract
tests, and compare both bridges on every accepted development action, retaining
the same agents/ledger/score and exact zero road footprint. No protected solver
or protected score is run by preflight; no fixture is edited or selected out.

The sole current protected entry point becomes claim_cap_protected_347.py. It
reuses the unchanged347 normalization, unchanged344 runner/summarizer and every
original gate. A create-only protected execution manifest changes only the bridge
selection and adds explicit provenance for this amendment, its tests and prior
qualification. All original dependency hashes remain present and unchanged.
Prior development/sealed qualification is checked against its exact original
execution and summary hashes, not bypassed or relabeled under a new hash.
The original protected bridge call is superseded for current execution; there is
no alternate solver, retry fallback or reduced validator. All current callers,
STATE and the single monitor must use this repaired protected entry point.

Only after this unscored preflight passes may the same protected directory resume
with NEW stdout/stderr companions. Preserve108fixtures/432sides/216fixed-label
pairs/2736ACK/2304transitions, all binary/config/phase order/deadline/work limits,
same no-gain causal audits and full preregistered acceptance gate. No arbitrary
RAM floor, VM build or product change. Completed sealed qualification remains
valid; full protected/lane-champion/integration/genuineBTC gates remain necessary.
